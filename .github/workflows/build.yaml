name: "build_deploy_app"

on:
    push:
        branches:
            - master

jobs:
    # server:
    #     name: "Build Server"
    #     runs-on: ubuntu-latest
    #     steps:
    #         - name: "Build:checkout"
    #           uses: actions/checkout@v2
    #         - name: "Build:dockerimage"
    #           uses: docker/build-push-action@v2
    #           with:
    #               push: true
    #               registry: ghcr.io
    #               username: "DavidIlie"
    #               password: ${{ secrets.GHCR_PASSWORD }}
    #               repository: davidilie/sharex-media-server
    #               tags: server
    #               file: server/Dockerfile
    web:
        name: "Build Web"
        runs-on: ubuntu-latest
        steps:
            - name: "Build:checkout"
              uses: actions/checkout@v2
            - name: Login to GitHub Container Registry
              uses: docker/login-action@v1
              with:
                  registry: ghcr.io
                  username: DavidIlie
                  password: ${{ secrets.GHCR_PASSWORD }}
            - name: Build and publishe Docker image
              run: |
                  docker build -t ghcr.io/davidilie/sharex-media-server:server -f server/Dockerfile .
                  docker push ghcr.io/davidilie/sharex-media-server:server
