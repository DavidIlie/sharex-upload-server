const SavedText = (): JSX.Element => {
    return (
        <div className={`text-sm text-gray-600 dark:text-dark-gray-300 mr-3 `}>
            Saved.
        </div>
    );
};

export default SavedText;
